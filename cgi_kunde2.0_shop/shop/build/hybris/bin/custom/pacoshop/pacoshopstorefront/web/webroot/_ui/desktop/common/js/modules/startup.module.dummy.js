$(document).ready(function(){

	/**
	 * Javascript to be run once the DOM has been loaded.
	 *
	 * Used only for module: dummy
	 *
	 */
	
	$(".mod-dummy").each(function() {
		var mod = $(this);

		// Module specific code
	    
	});
});